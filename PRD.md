# PRD: 회의록 → GitHub 이슈 자동화 에이전트 (가칭: MeetToIssue)

## 1. 개요

### 1.1 한 줄 요약
회의록을 붙여넣으면 AI 에이전트들이 할 일을 추출·정리·검수하여, 사용자 승인 후 GitHub 이슈로 일괄 생성해주는 개인 생산성 앱.

### 1.2 배경 / 문제 정의
- 회의가 끝나면 정리 담당자(서기, PM, 팀장, 개발 리드)는 회의록에서 할 일을 골라내고, 하나씩 GitHub 이슈로 옮겨 적는 데 **평균 20~30분**을 쓴다.
- 수작업 과정에서 **누락, 담당자 불명확, 기한 미기재, 완료 조건 모호** 같은 품질 문제가 반복된다.
- 이 반복 작업은 매 회의마다 발생하며, 개인의 집중 시간을 잠식한다.

### 1.3 목표
> **"회의 정리 30분을 1분으로"** — 회의록 입력부터 이슈 생성까지 1분 이내, 사용자는 검토·승인만 한다.

### 1.4 대상 사용자
- 주 3회 이상 회의에 참석하고 후속 작업 정리를 담당하는 **개인** (PM, 팀 리드, 개발자)
- GitHub로 업무를 관리하는 팀에 속한 사람

## 2. 필수 기술 요건 (해커톤)
| 기술 | 활용 방식 |
| --- | --- |
| **GitHub Copilot SDK** | 모든 에이전트의 LLM 모델 연결 |
| **Microsoft Agent Framework** | 에이전트 설계, 순차/병렬 오케스트레이션, GitHub 도구 호출, 컨텍스트 전달, 스트리밍 응답 |
| **Azure** | Container Apps(백엔드 배포), **Azure OpenAI**(모델 추론), Key Vault(비밀 관리), App Insights(관찰성), Bicep(반복 가능한 배포) |

### 2.1 기술 스택
| 레이어 | 선택 | 비고 |
|---|---|---|
| 백엔드 | **Python 3.12 + FastAPI** | `backend/` 디렉터리. SSE 스트리밍, Pydantic 구조화 출력 |
| 에이전트 | `agent-framework-core` + `agent-framework-orchestrations` | `WorkflowBuilder` 순차 오케스트레이션 |
| 모델 연결 | `MODEL_PROVIDER` 설정으로 전환 | `azure_openai`(기본) / `copilot_sdk` |
| GitHub 연동 | `httpx` 비동기 클라이언트 | 리포·라벨 조회, 이슈 일괄 생성 |
| 이력 | SQLite | 요약만 저장, 원문 미저장 |
| 프론트 | 반응형 웹 (별도 작업) | SSE 소비 |
| 패키지 관리 | `uv` | |

> **모델 공급자 이중화**: `github-copilot-sdk`는 Copilot CLI를 JSON-RPC로 제어하는
> 에이전트 SDK라 서버 파이프라인에는 제약이 있다. 그래서 기본값은 구조화 출력이
> 안정적인 Azure OpenAI(`agent-framework-openai`의 `OpenAIChatCompletionClient` +
> `azure_endpoint`)로 두고, `copilot_sdk` 경로도 함께 제공한다. 두 경로 모두
> Agent Framework의 `SupportsAgentRun` 계약을 만족하므로 오케스트레이터 코드는 동일하다.
>
> **GitHub Models 폐지 대응**: 초안에서는 GitHub Models를 기본 공급자로 잡았으나,
> 실제 호출 검증 중 `410 github_models_retirement_brownout`을 확인했다.
> GitHub Models는 2026-07-30자로 완전 폐지됐다. 공급자 추상화를 미리 둔 덕분에
> **오케스트레이터·에이전트·API 코드는 그대로 두고 연결 계층과 설정만 교체**해
> Azure OpenAI로 이전했다. 인증은 API 키 없이 관리 ID(Entra ID)를 사용하며,
> Bicep이 Azure OpenAI 계정·모델 배포·`Cognitive Services OpenAI User` 역할을
> 함께 프로비저닝한다(`disableLocalAuth: true`로 키 발급 자체를 차단).

## 3. 사용자 시나리오 (핵심 플로우)

### 3.1 화면 내비게이션 구조

```
[로그인/리포 선택]
      │
      ├─ 이슈 탭 ──→ [이슈 목록] ─[+ 이슈 추가]→ [회의록 입력] → [분석·검수] → [승인] → [생성 결과]
      │                    ▲                                                              │
      │                    └──────────────────────────────────────────────────────────────┘
      │
      └─ PR 탭 ───→ [PR 목록] ─[PR 선택]→ [PR 상세 + 리뷰 실행] → [리포트 검토] → [코멘트 게시]
```

### 3.2 이슈 플로우 (MVP)
1. **리포 선택**: 접근 가능한 GitHub 리포 목록에서 선택
2. **이슈 목록**: 해당 리포의 기존 이슈 조회 (상태·라벨 필터), 우측 상단 **[+ 회의록으로 이슈 만들기]**
3. **회의록 입력**: 텍스트 붙여넣기 → **[분석 시작]**
4. **분석 진행**: 에이전트 진행 상황이 **실시간 스트리밍**으로 표시
   - 🔍 추출 에이전트: 결정사항 / 할 일 / 담당자 / 기한
   - 📝 정리 에이전트: 이슈 초안 변환 (제목, 본문, 완료 조건, 라벨)
   - ✅ 검수 에이전트: 누락·중복·모호 항목 지적
5. **승인**: 초안 카드 **수정 / 제외 / 선택** → **[승인 및 생성]**
6. **결과**: 생성된 이슈 링크 목록 표시 → 이슈 목록으로 복귀(신규 이슈 하이라이트)

### 3.3 PR 플로우 (확장)
1. **리포 선택** (이슈 플로우와 공유)
2. **PR 목록**: 열린 PR 조회 (작성자·상태 필터)
3. **PR 선택**: PR 상세(변경 파일, 연결 이슈) 확인 → **[AI 리뷰 실행]**
4. **리뷰 진행**: 3개 에이전트 **병렬** 스트리밍
   - 📋 요구사항 대조: 연결 이슈의 AC ↔ diff
   - 🔎 코드 품질: 버그·스타일
   - 📄 변경 요약: 요약 + 학습 포인트
5. **리포트 검토**: 항목별 채택/제외 선택
6. **게시**: **[코멘트 게시]** → PR에 코멘트 등록 (AI 생성 표시 포함)

## 4. 기능 요구사항

### 4.1 MVP (필수)
| ID | 기능 | 설명 | 우선순위 |
| --- | --- | --- | --- |
| F1 | 리포 선택 | GitHub 리포 목록 조회·선택, 최근 사용 리포 기억 | P0 |
| F2 | 이슈 목록 | 선택한 리포의 이슈 조회 (상태·라벨 필터), 신규 생성 진입점 | P0 |
| F3 | 회의록 입력 | 텍스트 붙여넣기 (파일 업로드는 선택) | P0 |
| F4 | 추출 에이전트 | 결정사항·할 일·담당자·기한 구조화 추출, 스트리밍 출력 | P0 |
| F5 | 정리 에이전트 | 이슈 초안 생성: 제목, 본문, **완료 조건(AC)**, 라벨, 담당자 제안 | P0 |
| F6 | 검수 에이전트 | 누락 기한, 담당자 불명, 중복, 모호한 AC 지적 | P0 |
| F7 | 승인 UI | 초안 카드 수정/제외/선택, 승인 전 생성 불가 | P0 |
| F8 | 이슈 생성 | GitHub API로 일괄 생성, 결과 링크 표시, 부분 실패 처리 | P0 |
| F9 | 이력 | 과거 회의 분석 이력 목록 | P1 |

### 4.2 확장 (시간 여유 시): PR 셀프 리뷰
| ID | 기능 | 설명 |
| --- | --- | --- |
| E1 | PR 목록 | 선택한 리포의 열린 PR 조회 (작성자·상태 필터) |
| E2 | PR 상세 | 변경 파일 요약, 연결된 이슈 표시, **[AI 리뷰 실행]** |
| E3 | 요구사항 대조 에이전트 | 연결된 이슈의 AC ↔ PR diff 비교, 충족 여부 체크리스트 |
| E4 | 코드 품질 에이전트 | 버그·스타일 지적 (E3과 병렬 실행) |
| E5 | 요약 에이전트 | 변경 요약 + 학습 포인트 (E3, E4와 병렬) |
| E6 | 리포트 게시 | 항목별 채택/제외 후 사용자 확인 → PR 코멘트 게시 (AI 생성 명시) |

> 설계 포인트: F5에서 AC를 구조화해 이슈 본문에 넣어두면, E3가 그대로 재사용 → 코어와 확장이 자연스럽게 연결됨.

## 5. 에이전트 아키텍처

```
[사용자] → [웹 프론트 (반응형)]
              │ SSE 스트리밍
              ▼
[백엔드 API — FastAPI (Azure Container Apps)]
              │
              ▼
[오케스트레이터 — Microsoft Agent Framework: WorkflowBuilder]
   ├─ 1. 추출 에이전트 (extract)  → ExtractionResult
   ├─ 2. 정리 에이전트 (compose)  → CompositionResult
   ├─ 3. 검수 에이전트 (review)   → ReviewResult
   └─ 컨텍스트: 리포의 실제 라벨·담당자 목록 (GitHub API)
              │
              ▼
[GitHub]  [Azure Key Vault: 토큰]  [App Insights: 로그/트레이스]
```

- 추출→정리→검수는 **순차 오케스트레이션** — 각 단계 출력이 다음 단계 입력
  (`WorkflowBuilder(start_executor=extract).add_edge(extract, compose).add_edge(compose, review)`)
- 각 단계는 **Pydantic 스키마로 구조화 출력**을 강제한다 (`response_format`)
- 실행 중 `ctx.add_event()`로 `StageEvent`(진행 상황)와 `DeltaEvent`(토큰)를 발행하고,
  API 계층이 `workflow.run(stream=True)`를 구독해 SSE 프레임으로 중계한다
- 확장 단계의 E2/E3/E4는 `ConcurrentBuilder`로 **병렬 오케스트레이션**

### 5.1 안전장치 (코드 레벨)
- **환각 필터**: 정리 에이전트가 지어낸 라벨·담당자는 리포의 실제 목록과 대조해 제거
- **단계별 재시도**: 각 에이전트 단계는 독립적으로 재시도되며, 시도 횟수가 이벤트로 노출됨
- **입력 격리**: 회의록은 구분자로 감싸 데이터로만 취급하고, 구분자 위조 시도를 무력화
- **접근 제어**: 서버가 자신의 GitHub PAT로 이슈를 생성하므로, `/api`는 공유 키(`X-API-Key`)로 보호한다. 운영 환경에서 키가 비어 있으면 열린 채로 뜨는 대신 요청을 거부한다
- **중단 안전성**: 사용자가 분석 도중 창을 닫으면 SSE 스트림을 조용히 종료한다(끊긴 연결에 추가 프레임을 쓰지 않음)

## 6. 비기능 요구사항

| 항목 | 요구사항 |
| --- | --- |
| 성능 | 회의록 2,000자 기준 분석 완료 60초 이내, 첫 토큰 3초 이내 표시 |
| 플랫폼 | 반응형 웹 (데스크톱/모바일) |
| 오류 처리 | 에이전트 실패 시 단계별 재시도(기본 2회), 이슈 생성 부분 실패 시 성공/실패 구분 표시, 라벨 오류 시 라벨 없이 재시도 |
| 배포 | Bicep + `deploy.sh`로 반복 가능한 배포, 리포에 IaC 포함 (`backend/infra/`) |
| 관찰성 | App Insights로 에이전트 단계별 소요 시간·실패율 추적 |

## 7. 책임 있는 AI / 보안

- **AI 생성 표시**: 모든 초안·이슈 본문에 "🤖 AI가 생성한 초안입니다" 명시
- **사용자 승인 필수**: 이슈 생성(외부 부작용)은 반드시 승인 후 실행 — 자동 생성 없음.
  승인 검사는 토큰 검사보다 **먼저** 수행해, 어떤 상황에서도 승인 없이 생성 경로에 진입할 수 없다
- **근거 제시**: 추출된 할 일마다 회의록 원문 근거 문장 인용 필수 (근거 없으면 추출하지 않음)
- **프롬프트 인젝션 대응**: 회의록은 구분자로 감싼 데이터로만 취급하고, 구분자 위조 시도를 무력화.
  시스템 프롬프트에 지시 무시 규칙 명시
- **비밀 관리**: GitHub 토큰은 Azure Key Vault, 클라이언트에 노출 금지, 코드에 하드코딩 금지.
  Container Apps는 관리 ID로 비밀번호 없이 Key Vault·ACR에 접근
- **개인정보**: 회의록 원문은 **저장하지 않는다**. 이력에는 요약·초안 메타데이터·이슈 링크만 남는다.
  `PERSIST_TRANSCRIPT=true`로 사용자가 명시적으로 동의한 경우에만 원문을 저장하며,
  `DELETE /api/history/{id}`로 삭제할 수 있다

## 8. 성공 지표 (데모용)

| 지표 | 목표 |
| --- | --- |
| 회의 정리 소요 시간 | 30분 → 1분 (수작업 대비 시연으로 입증) |
| 할 일 추출 재현율 | 데모 회의록 기준 누락 0건 |
| 엔드투엔드 성공 | 데모 중 입력→이슈 생성까지 무중단 시연 |

## 9. 데모 시나리오 (3분)

1. (30초) 문제 소개: "회의 끝나면 이걸 다 손으로 옮깁니다"
2. (60초) 가짜 회의록 붙여넣기 → 3개 에이전트 스트리밍 시연
3. (45초) 검수 지적 확인 → 초안 1개 수정 → 승인 → 이슈 3건 생성, GitHub에서 확인
   - **일부러 실패 케이스 포함**: 권한 없는 리포에 1건을 섞어 부분 실패 UI를 보여준다
     (평가 기준 4번 "오류 처리"를 말이 아니라 화면으로 입증)
4. (30초) Azure 대시보드(App Insights)로 관찰성 + 아키텍처 한 장
5. (15초) 확장 비전: PR 셀프 리뷰

## 10. 마일스톤

| 단계 | 내용 |
| --- | --- |
| M1 | 백엔드 스캐폴드 + Copilot SDK 연결 + 추출 에이전트 1개 동작 |
| M2 | 3-에이전트 오케스트레이션 + 스트리밍 API |
| M3 | 프론트: 입력 → 스트리밍 표시 → 승인 UI |
| M4 | GitHub 이슈 생성 + 부분 실패 처리 |
| M5 | Key Vault + App Insights 연결, 전체 재배포 |
| M6 | 데모 리허설(성능 실측 포함), 여유 시 확장(PR 리뷰) 착수 |

> M2.5를 넣은 이유: 해커톤에서 배포는 거의 항상 예상보다 오래 걸린다.
> 파이프라인이 완성되기 전에 배포 경로를 한 번 뚫어두면 마지막 날 리스크가 사라진다.

## 11. 평가 기준 매핑

| 평가 항목 (가중치) | 대응 |
| --- | --- |
| SDK + Agent Framework (25%) | 3+ 에이전트 순차/병렬 오케스트레이션, 도구 호출, 스트리밍 — §5 |
| 생산성 효과 (18%) | "30분→1분" 전후 비교, 명확한 개인 사용자 — §1, §8 |
| Azure (18%) | Container Apps + Key Vault + App Insights, IaC 배포, 과잉 서비스 없음 — §2, §6 |
| 완성도 (16%) | 좁은 스코프로 엔드투엔드 완성, 오류 처리, 반응형 웹 — §4, §6 |
| UX (12%) | 스트리밍 진행 표시, 승인 카드 UI, 근거 제시 — §3 |
| 책임 있는 AI (6%) | 승인 게이트, AI 표시, 근거 인용, Key Vault — §7 |
| 혁신성 (5%) | 단순 요약이 아닌 "추출→구조화→검수→실행" 폐루프 + AC 기반 확장 설계 — §4.2 |

---

## 12. 와이어프레임

### S1. 리포 선택
```
┌───────────────────────────────────────────────────────────┐
│  🤖 MeetToIssue                          [@octocat  ▾]    │
├───────────────────────────────────────────────────────────┤
│                                                           │
│   리포지토리를 선택하세요                                 │
│   ┌─────────────────────────────────────────────────┐     │
│   │ 🔍  리포 검색...                                │     │
│   └─────────────────────────────────────────────────┘     │
│                                                           │
│   최근 사용                                               │
│   ┌─────────────────────────────────────────────────┐     │
│   │ 📦 my-team/backend-api      ⭐ 이슈 12 · PR 3   │  →  │
│   ├─────────────────────────────────────────────────┤     │
│   │ 📦 my-team/web-client          이슈 5  · PR 1   │  →  │
│   └─────────────────────────────────────────────────┘     │
│   전체 리포                                               │
│   ┌─────────────────────────────────────────────────┐     │
│   │ 📦 octocat/hello-world         이슈 0  · PR 0   │  →  │
│   └─────────────────────────────────────────────────┘     │
└───────────────────────────────────────────────────────────┘
```

### S2. 이슈 목록  (리포 선택 후 기본 화면)
```
┌───────────────────────────────────────────────────────────┐
│ ← my-team/backend-api ▾    [ 이슈 ]  PR        [@octocat] │
├───────────────────────────────────────────────────────────┤
│  이슈 12건            ┌──────────────────────────────────┐ │
│  [열림▾][라벨▾][🔍]   │ ✨ 회의록으로 이슈 만들기        │ │
│                       └──────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ ○ #42  로그인 토큰 만료 처리                          │ │
│  │        bug · @kim · 3일 전                            │ │
│  ├───────────────────────────────────────────────────────┤ │
│  │ ○ #41  검색 API 페이지네이션 추가       🤖 AI 생성    │ │
│  │        feature · @lee · 5일 전                        │ │
│  ├───────────────────────────────────────────────────────┤ │
│  │ ● #38  배포 스크립트 정리                             │ │
│  │        chore · 담당자 없음 · 1주 전                   │ │
│  └───────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────┘
```

### S3. 회의록 입력
```
┌───────────────────────────────────────────────────────────┐
│ ← 이슈 목록으로            회의록으로 이슈 만들기          │
├───────────────────────────────────────────────────────────┤
│  ①입력 ──── ②분석 ──── ③검토 ──── ④생성                  │
│                                                           │
│  회의록을 붙여넣으세요                                    │
│  ┌───────────────────────────────────────────────────┐    │
│  │ 8/22 스프린트 회의                                │    │
│  │ 참석: 김, 이, 박                                  │    │
│  │ - 로그인 토큰 만료 버그 → 김이 이번 주까지       │    │
│  │ - 검색 API 페이지네이션 필요 (이)                 │    │
│  │ - 배포 스크립트 정리하기로 함                     │    │
│  │                                                   │    │
│  │                                       1,240자     │    │
│  └───────────────────────────────────────────────────┘    │
│  📎 파일 첨부 (.txt, .md)      🔒 원문은 저장되지 않습니다│
│                                                           │
│                             [ 취소 ]  [ ✨ 분석 시작 ]    │
└───────────────────────────────────────────────────────────┘
```

### S4. 분석 진행 (스트리밍)
```
┌───────────────────────────────────────────────────────────┐
│  ①입력 ──── ●②분석 ──── ③검토 ──── ④생성      [ 중단 ]   │
├───────────────────────────────────────────────────────────┤
│ ┌── 에이전트 진행 ──────┐ ┌── 실시간 결과 ──────────────┐ │
│ │ ✅ 🔍 추출 에이전트    │ │ 결정사항 2건                │ │
│ │    할 일 3건 추출      │ │  • 토큰 만료 정책 24h 확정  │ │
│ │      1.4초             │ │  • 배포 주기 주 1회         │ │
│ │                        │ │                             │ │
│ │ ⏳ 📝 정리 에이전트    │ │ 할 일 3건                   │ │
│ │    이슈 초안 2/3...    │ │  1. 토큰 만료 버그  @김 8/24│ │
│ │    ▓▓▓▓▓▓▓░░░          │ │  2. 검색 페이지네이션 @이   │ │
│ │                        │ │  3. 배포 스크립트 정리 —    │ │
│ │ ○ ✅ 검수 에이전트     │ │                             │ │
│ │    대기 중             │ │ ▌생성 중...                 │ │
│ └────────────────────────┘ └─────────────────────────────┘ │
│  🤖 AI가 생성 중입니다. 결과는 검토 후 반영됩니다.        │
└───────────────────────────────────────────────────────────┘
```

### S5. 검토 · 승인
```
┌───────────────────────────────────────────────────────────┐
│  ①입력 ──── ②분석 ──── ●③검토 ──── ④생성                 │
├───────────────────────────────────────────────────────────┤
│  ⚠️ 검수 결과: 기한 없는 항목 1건, 담당자 미지정 1건      │
│                                                           │
│  이슈 초안 3건        ☑ 전체 선택          🤖 AI 생성 초안│
│  ┌───────────────────────────────────────────────────────┐│
│  │ ☑  로그인 토큰 만료 처리 버그 수정          [✏️][🗑]  ││
│  │    라벨: bug   담당: @kim   기한: 8/24                ││
│  │    ▸ 완료 조건                                        ││
│  │      □ 토큰 만료 시 401 반환                          ││
│  │      □ 리프레시 토큰 재발급 동작                      ││
│  │    ▸ 📄 근거: "로그인 토큰 만료 버그 → 김이 이번 주…" ││
│  ├───────────────────────────────────────────────────────┤│
│  │ ☑  검색 API 페이지네이션 추가               [✏️][🗑]  ││
│  │    라벨: feature   담당: @lee   기한: —               ││
│  │    ⚠️ 기한이 명시되지 않았습니다        [기한 설정]   ││
│  ├───────────────────────────────────────────────────────┤│
│  │ ☐  배포 스크립트 정리                       [✏️][🗑]  ││
│  │    ⚠️ 담당자 미지정 · 완료 조건이 모호합니다          ││
│  └───────────────────────────────────────────────────────┘│
│                                                           │
│  선택 2건이 my-team/backend-api에 생성됩니다              │
│                    [ ← 다시 분석 ]  [ ✅ 승인 및 생성 ]   │
└───────────────────────────────────────────────────────────┘
```

### S6. 생성 결과
```
┌───────────────────────────────────────────────────────────┐
│  ①입력 ──── ②분석 ──── ③검토 ──── ●④생성                 │
├───────────────────────────────────────────────────────────┤
│            ✅ 이슈 2건이 생성되었습니다                   │
│                                                           │
│   ┌─────────────────────────────────────────────────┐     │
│   │ ✅ #43 로그인 토큰 만료 처리 버그 수정   [열기↗]│     │
│   │ ✅ #44 검색 API 페이지네이션 추가        [열기↗]│     │
│   │ ⚠️ 배포 스크립트 정리 — 제외됨                  │     │
│   └─────────────────────────────────────────────────┘     │
│                                                           │
│   ⏱ 소요 42초 · 수작업 예상 28분  →  97% 절감            │
│                                                           │
│           [ 이슈 목록으로 ]  [ 새 회의록 분석 ]           │
└───────────────────────────────────────────────────────────┘
```

### S7. PR 목록 (확장)
```
┌───────────────────────────────────────────────────────────┐
│ ← my-team/backend-api ▾     이슈  [ PR ]       [@octocat] │
├───────────────────────────────────────────────────────────┤
│  열린 PR 3건        [열림▾][작성자▾][🔍]                  │
│  ┌───────────────────────────────────────────────────────┐│
│  │ 🔀 #57  토큰 만료 처리 구현                           ││
│  │        @kim · +142 −38 · 🔗 #43 연결   [ AI 리뷰 ▸ ] ││
│  ├───────────────────────────────────────────────────────┤│
│  │ 🔀 #56  검색 페이지네이션                             ││
│  │        @lee · +88 −12 · 연결 이슈 없음  [ AI 리뷰 ▸ ]││
│  └───────────────────────────────────────────────────────┘│
└───────────────────────────────────────────────────────────┘
```

### S8. PR 리뷰 리포트 (확장)
```
┌───────────────────────────────────────────────────────────┐
│ ← PR 목록      #57 토큰 만료 처리 구현   🔗 이슈 #43      │
├───────────────────────────────────────────────────────────┤
│ ┌── 병렬 실행 ─────────────────────────────────────────┐  │
│ │ ✅ 📋 요구사항 대조   ✅ 🔎 코드 품질   ⏳ 📄 요약   │  │
│ └──────────────────────────────────────────────────────┘  │
│                                                           │
│  📋 요구사항 대조 (이슈 #43의 완료 조건)          ☑ 채택 │
│  ┌───────────────────────────────────────────────────────┐│
│  │ ✅ 토큰 만료 시 401 반환    → auth.ts:42              ││
│  │ ❌ 리프레시 토큰 재발급     → 관련 변경 없음          ││
│  │    ⚠️ 회의에서 정한 조건 1건이 미충족입니다           ││
│  └───────────────────────────────────────────────────────┘│
│  🔎 코드 품질 2건                                 ☑ 채택 │
│  ┌───────────────────────────────────────────────────────┐│
│  │ ⚠️ auth.ts:57  에러 케이스에서 토큰이 로그에 노출     ││
│  │ 💡 auth.ts:12  중복 상수 → 상수 파일로 분리 권장      ││
│  └───────────────────────────────────────────────────────┘│
│  📄 변경 요약 · 학습 포인트                       ☐ 채택 │
│                                                           │
│  🤖 AI가 생성한 리뷰입니다. 게시 전 확인하세요.           │
│                  [ 복사 ]  [ 💬 PR에 코멘트 게시 ]        │
└───────────────────────────────────────────────────────────┘
```

### 모바일 (반응형) — S5 검토 화면 예시
```
┌─────────────────────┐
│ ← 검토      3/4 단계│
├─────────────────────┤
│ ⚠️ 기한 없음 1건    │
│ ┌─────────────────┐ │
│ │☑ 로그인 토큰    │ │
│ │  만료 처리      │ │
│ │  bug·@kim·8/24  │ │
│ │  ▸ 완료조건 2   │ │
│ │  ▸ 📄 근거      │ │
│ │        [✏️][🗑] │ │
│ ├─────────────────┤ │
│ │☑ 검색 API       │ │
│ │  페이지네이션   │ │
│ │  ⚠️ 기한 없음   │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ ✅ 승인 및 생성 │ │
│ └─────────────────┘ │
└─────────────────────┘
```

### UX 설계 원칙
| 원칙 | 적용 |
| --- | --- |
| 통제권 | 생성·게시는 항상 사용자 승인 후. 카드 단위 수정/제외 가능 |
| 투명성 | 각 초안에 회의록 원문 **근거 인용** 표시, AI 생성 배지 |
| 지연 처리 | 단계별 진행 표시 + 토큰 스트리밍, 중단 버튼 제공 |
| 오류 처리 | 단계 실패 시 해당 단계만 재시도, 이슈 생성 부분 실패는 성공/실패 구분 표시 |
| 접근성 | 상태를 색+아이콘+텍스트로 중복 표기, 키보드 내비게이션, ARIA live로 스트리밍 안내 |
| 효과 가시화 | 결과 화면에 소요 시간 vs 수작업 예상 시간 비교 (평가 항목 2 어필) |
