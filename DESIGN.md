# MeetToIssue 디자인 가이드 — "Desk"

회의 노트를 옮겨 적는 **책상** 위를 그대로 화면으로 옮겼습니다.
따뜻한 종이 위에 잉크로 쓰고, 도장처럼 한 가지 색만 눌러 강조합니다.

모든 값의 원본은 [`web/src/styles/tokens.css`](web/src/styles/tokens.css) 입니다.
이 문서는 그 토큰을 **왜** 그렇게 정했고 **어떻게** 쓰는지를 적습니다.

---

## 1. 원칙

| 원칙 | 이유 |
| --- | --- |
| 강조색은 **딱 하나** (clay) | 색이 많아지면 어디를 눌러야 할지 모릅니다. 누를 곳만 clay 로 칠합니다. |
| 라운드는 **최대 6px** | 크게 둥근 파스텔 카드는 요즘 생성 UI의 기본값입니다. 각을 살려 문서에 가깝게 둡니다. |
| 그림자 대신 **1px 라인** | 떠 있는 느낌보다 종이에 그은 선이 회의록과 어울립니다. 그림자는 모달·팝오버에만. |
| 제목은 **세리프** | 본문과 위계가 분명해지고, 화면이 도구가 아니라 기록물처럼 보입니다. |
| 아이콘은 **스트로크 SVG** | 이모지를 UI 아이콘으로 쓰지 않습니다. 플랫폼마다 모양이 달라지고 톤이 무너집니다. |
| 애니메이션은 **150–220ms** | 진행 표시 외에는 눈에 띄지 않게. 튀는 이징을 쓰지 않습니다. |

### 쓰지 않는 것

- 파랑·보라 그라디언트, 네온 글로우
- 큰 라운드(12px+) 파스텔 카드
- 이모지 아이콘
- pill 버튼 남용, 알약형 태그의 과채도
- 3D 아이소메트릭 일러스트

---

## 2. 색

### 중립 — 종이와 잉크

| 토큰 | 값 | 쓰임 |
| --- | --- | --- |
| `--paper` | `#f7f5f0` | 앱 배경 |
| `--paper-sunk` | `#efece5` | 한 단계 들어간 면 (코드블록, 트랙) |
| `--surface` | `#fffefb` | 카드·패널 |
| `--surface-hover` | `#f3f0e9` | 행 호버 |
| `--ink-900` | `#1b1a17` | 제목 |
| `--ink-700` | `#3a3833` | 본문 |
| `--ink-500` | `#6d6a62` | 보조 텍스트 |
| `--ink-400` | `#8d8981` | 플레이스홀더 |
| `--ink-200` | `#c9c4b9` | 비활성 |
| `--line` / `--line-strong` / `--line-soft` | `#ddd8cd` / `#c4bdae` / `#ebe7dd` | 경계선 3단계 |

순백(`#fff`)과 순흑(`#000`)을 쓰지 않습니다. 종이는 약간 노랗고 잉크는 약간 갈색입니다.

### 강조 — 클레이 (벽돌빛)

```
--clay-700 #7d3a22   --clay-600 #96482c   --clay #a9532f (primary)
--clay-300 #cf9a7c   --clay-200 #e8d3c6   --clay-100 #f4e7de   --clay-050 #faf1eb
```

`--clay` 는 **주 동작 버튼, 선택된 탭·행, 진행 중 표시**에만 씁니다.
장식으로 칠하지 않습니다.

### 상태 — 같은 온도, 낮은 채도

| 토큰 | 값 | 뜻 |
| --- | --- | --- |
| `--moss` | `#4a6b43` | 성공 / open / merged 완료 |
| `--ochre` | `#8d6c1e` | 주의 · 검수 경고 |
| `--brick` | `#9c3a33` | 오류 / closed |
| `--slate` | `#416072` | 중립 정보 · draft |

각각 `-700`(텍스트), `-100`(테두리), `-050`(배경) 변형이 있습니다.
상태색도 채도를 낮춰 종이 위에 얹혀 보이도록 맞췄습니다.

---

## 3. 타이포

| 역할 | 스택 | 토큰 |
| --- | --- | --- |
| 제목 | Iowan Old Style → Garamond → Georgia → Noto Serif KR | `--font-serif` |
| 본문·UI | Pretendard → system-ui | `--font-sans` |
| 수치·식별자·코드 | ui-monospace → SF Mono → JetBrains Mono | `--font-mono` |

- Pretendard 는 `index.html` 에서 jsdelivr CDN 으로 불러옵니다.
- 이슈 번호(`#147`), 브랜치명, 시간(`0.6s`), diff(`+42 −8`) 는 **모노**로 씁니다. 스캔이 쉬워집니다.
- 크기: `--fs-11` ~ `--fs-28`. 본문 기본은 `--fs-14`, 화면 제목은 `--fs-24` 세리프.

---

## 4. 형태와 간격

```
--r-sm 3px   --r-md 5px   --r-lg 6px      /* 라운드는 여기까지 */
--sp-1 4px … --sp-12 48px                 /* 4px 그리드 */
--header-h 52px      --content-max 1080px
--shadow-pop / --shadow-modal             /* 팝오버·모달 전용 */
--focus: 0 0 0 3px var(--clay-100)        /* 포커스 링 */
```

포커스 링은 **항상** 보입니다. 키보드로 전체 플로우를 돌 수 있어야 합니다.

---

## 5. 컴포넌트 사용 규칙

UI 키트는 `web/src/components/ui/` 에 있고 `components/ui/index.ts` 로 한 번에 가져옵니다.

| 컴포넌트 | 언제 쓰나 |
| --- | --- |
| `Button` (`primary`/`default`/`ghost`/`danger`) | 화면당 `primary` 는 하나. 나머지는 `default` 또는 `ghost`. |
| `IconButton` | 라벨이 없어도 뜻이 분명한 동작만. `aria-label` 필수. |
| `Field` / `TextInput` / `TextArea` | 라벨은 항상 위, 도움말은 아래. placeholder 를 라벨 대신 쓰지 않습니다. |
| `Tag` | 라벨·상태 표시. 한 줄에 3개를 넘기지 않습니다. |
| `Card` + `CardHead`/`CardBody`/`CardFoot` | 독립적으로 읽히는 묶음. |
| `List` / `ListRow` | 같은 종류가 여럿일 때. 행 전체가 클릭 대상이면 `ListRow`. |
| `Tabs` | 같은 대상의 다른 면. 3~4개까지. |
| `HoverMenu` | 호버로 열리는 추가 메뉴. 클릭·포커스로도 열리고 Escape 로 닫힙니다. |
| `Modal` | 되돌릴 수 없거나 흐름을 끊어야 하는 선택만. |
| `EmptyState` | 비어 있는 이유와 다음에 할 일을 같이 적습니다. |
| `Banner` / `useToast` | 결과 알림. 오류는 배너, 성공은 토스트. |

### 마이크로카피

- 버튼은 **동사로 끝냅니다**: "회의록 읽어내기", "4건 이슈로 만들기"
- 빈 화면은 사용자를 탓하지 않습니다: "방금 끝난 회의가 있나요?"
- 되돌릴 수 없는 동작은 미리 알립니다: "승인하기 전까지 GitHub 에는 아무것도 만들어지지 않습니다."
- 문장에 느낌표를 쓰지 않습니다.

---

## 6. 접근성 체크

- [x] 본문/배경 대비 `--ink-700` on `--surface` ≈ 10:1
- [x] `--clay` on `--surface` ≈ 5.1:1 (버튼 텍스트는 흰색, 배경 clay 로 뒤집음)
- [x] 모든 인터랙티브 요소에 포커스 링
- [x] `ListRow` 는 `role="button"` + Enter/Space 처리
- [x] 아이콘 전용 버튼에 `aria-label`
- [x] 탭은 `role="tab"` / `aria-selected`
